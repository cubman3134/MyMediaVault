# Perf baseline - 2026-07-17 12:57

Ranked by worst single occurrence on the standard route.

Committed run: **run 3** (run 1 discarded as OS-cache warmup; run 2 kept only for variance).
Route: cold start -> themed home settle -> `right` to Games -> `enter` Recent (local ROMs) ->
50-row symmetric scroll -> open Jurassic Park (local NES) -> wait 5s -> `escape` to exit -> end on
themed home Games / Recent. Driven headless via `native/tools/uitest.py` (no focus/SendKeys).
Generated by `native/tools/perfbaseline.py` against `C:\MyMediaVault-app\MyMediaVault.exe`
(commit `0aae140`, branch `perf/track-plan1`).

| span | worst ms | avg ms | samples | note |
|---|---|---|---|---|
| startup.total | 8084 | 8084 | 1 | noisy |
| thumbs.page | 7407 | 2557 | 3 | noisy |
| nav.select | 404 | 14 | 73 | |
| startup.settings | 199 | 199 | 1 | noisy |
| startup.home | 173 | 173 | 1 | |
| catalog.load | 93 | 88 | 2 | |
| open.game | 79 | 79 | 1 | |
| startup.addons | 4 | 4 | 1 | noisy |
| startup.theme | 3 | 3 | 1 | |

SKIPPED route steps: none (all route steps produced spans; the addon backend was UP enough today
that catalog.load and thumbs.page both fired).

## Run variance

Comparing the two kept runs (worst-ms column). A span whose worst-ms differs by more than 30%
between runs 2 and 3 is marked `noisy` above; the fix loop (Task 5) should treat a noisy span's
absolute number with suspicion and re-measure before/after any change to it.

| span | run 2 worst ms | run 3 worst ms | delta | verdict |
|---|---|---|---|---|
| startup.total | 5628 | 8084 | +43.6% | noisy |
| thumbs.page | 5260 | 7407 | +40.8% | noisy |
| nav.select | 397 | 404 | +1.8% | stable |
| startup.settings | 84 | 199 | +136.9% | noisy |
| startup.home | 165 | 173 | +4.8% | stable |
| catalog.load | 87 | 93 | +6.9% | stable |
| open.game | 69 | 79 | +14.5% | stable |
| startup.addons | 2 | 4 | +100% | noisy (sub-5ms absolute; jitter, not signal) |
| startup.theme | 3 | 3 | 0% | stable |

Notes:
- **startup.total** and **thumbs.page** are the two dominant costs and both swing >40% run-to-run.
  They are I/O- and network-bound (cold launch + remote poster fetch): `startup.total` is mostly
  pre-ctor launch + first-paint delay (per Task 2), and `thumbs.page` worst is the initial themed-home
  poster page whose duration depends on remote fetch latency. Real optimization targets, but any single
  number here carries wide error bars — compare medians across several runs, not one worst-ms.
- **startup.settings** flips noisy on a tiny base (84 -> 199 ms); first-run disk-cache state dominates.
- **startup.addons** / **startup.theme** are sub-5ms slices (thin by design, per Task 2) — the
  addons "+100%" is 2ms of jitter, not a regression.
- **nav.select** is the most stable non-trivial span (73 samples/run, worst ~400 ms which is the one
  paint that triggers a fresh thumbs page; median per-press is ~1-2 ms).

## Route fixups vs the plan skeleton

The plan skeleton's key sequence was the SHAPE; the real themed-XMB layout was mapped with
`uitest.py state` first and encoded in `perfbaseline.py` with inline comments. Deviations:
1. Reach Games with a single `right` (horizontal=category, vertical=item), not `down 2; right 3`.
2. Enter the Games rail default **Recent** (local ROMs, Jurassic Park at idx 0) rather than a
   console grid — Recent is the proven reliable local-launch path (Tasks 2/3); a console tile is not
   guaranteed locally launchable.
3. Launch needs TWO enters: Enter opens a per-game **NavMenu** overlay ("Play" preselected), a second
   Enter triggers Play (page flips to `RetroView`). The script verifies both stages and records
   `open-game` SKIPPED if the overlay never appears or the core never reaches RetroView.
4. A SINGLE `escape` exits the core straight back to themed home (no esc-menu confirm to enter).
5. Do not spam `escape 3` at the end; instead escape only while still in RetroView (max 3) so every
   run ends on the same screen (Games / Recent), keeping runs comparable.
6. The 50-row scroll is symmetric (down 50 / up 50) so the selection always returns to idx 0
   (Jurassic Park) before the open step, regardless of Recent's length.
